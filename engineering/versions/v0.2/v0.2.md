# v0.2 — Full SDK adoption + extraction (everything but business logic)

*Last updated: 2026-06-20*

**Status:** 🚧 In Progress · **Started:** 2026-06-20 · **Completed:** —

> Extract everything reusable (host · data · web · identity · integration clients · test harness) to the
> wow-two SDK; keep only *how we use it* — domain meaning, Products/Servers CQRS, version-resolution policy,
> store predicates, settings values. Per-layer map: `v0.2-analysis.md`.

## Decisions

- **D1** — STAYS = business logic only; everything else extracts or adopts.
- **D2** — GitHub + GHCR clients extract (`Integrations.*`), token source parameterized.
- **D3** — snake_case everywhere (DB + JSON wire); frontend types flip in Iter 8.
- **D4** — migrate enum stored values to snake now (`002-snake-enums`).
- **D5** — clock → SDK `TimeProvider`, not NodaTime.
- **D6** — extract-all exceeds the 1-week box → v0.2 runs as one multi-week adoption cycle.
- **D7** — every extract is two-sided: build SDK leaf → publish → adopt.

### Iteration 1 — Foundation & data contracts

- [x] Ability to use the SDK entity contracts.
- [x] Ability to tell time through the SDK time provider.
- [x] Ability to persist enums as snake_case.

### Iteration 2 — Host floor

- [x] Ability to wire the host on the SDK boot floor.
- [x] Ability to keep the single-host SPA serving and anonymous health.

### Iteration 3 — Web contracts & results

- [x] Ability to consume the SDK response envelope.
- [x] Ability to map categorized failures to HTTP via the SDK.

### Iteration 4 — Presentation & validation

- [x] Ability to return trace-correlated problem details.
- [x] Ability to validate requests through the SDK.

### Iteration 5 — Identity

- [ ] Ability to run GitHub OAuth and cookie auth on the SDK providers.
- [ ] Ability to gate access via the SDK allowlist and default-deny policy.

### Iteration 6 — Integrations

- [ ] Ability to read GitHub metadata through the SDK client.
- [ ] Ability to probe container images through the SDK client.

### Iteration 7 — Test baseline

- [ ] Ability to run the test suite on the published SDK testing package.

### Iteration 8 — Frontend

- [ ] Ability to render the dashboard from the beta UI library.

### Iteration 9 — Verification

- [ ] Build all halves and run the suites green.
- [ ] Smoke the live app end to end.

## Log

- **2026-06-20** — Iter 4 (validation): app-request only — 3 command validators + `AddMediatorValidationBehavior` + `AddFluentValidatorsFromAssemblies`; sync checks out of handlers, async stays; ProblemDetails already via `AddApiDefaults`. New SDK fix `UseApiDefaults` → `UseExceptionHandler()` (registered handlers were dead) — **pending publish**, drydock bridges locally meanwhile. E2E `19/19`.
- **2026-06-20** — Iters 1–3 done on SDK `10.0.26-beta`. Extracted to the SDK: `EnumCaseConverter` parameterless ctor · anonymous `MapHealthChecks` · `Mediator.Result.FailureCategory` (8-case union, all 3 apps) + `ICategorizedFailure` · `Web.Results.ToStatusCode` · `Web.Contracts.ApiResponse`. 3 TEMP bridges finalized. Build + E2E `18/18`.
