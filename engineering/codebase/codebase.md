# codebase/

The code — the only place code lives.

| Dir | What |
|---|---|
| `drydock.backend-services/` | .NET 10 Clean-Arch solution (`Drydock.slnx`) — `Drydock.{Api,Application,Domain,Infrastructure,Persistence}` (+ `tests/` when added) |
| `drydock.frontend-services/` | React 19 + Vite + Tailwind v4 control-plane dashboard |

Build / run: see root `README.md`. Architecture: `../architecture/architecture.md`.
