# business/

Business logic & planning for Drydock — the *what* and *why*, separate from the `platform/` *how*.

- [`planning/drydock-roadmap.md`](planning/drydock-roadmap.md) — first-ship cut line + phasing + current status.

The full design spec (architecture, deploy flow, domain flow, data model, feature backlog) lives in
the workspace at `wow-two-ws/ideas/drydock-spec.md`.
